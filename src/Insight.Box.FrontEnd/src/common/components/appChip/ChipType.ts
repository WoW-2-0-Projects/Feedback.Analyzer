/*
 * Represents the chip type
 */
export enum ChipType {

    /* Indicates the chip type is success */
    Success ,

    /* Indicates the chip type is error */
    Error ,

    /* Indicates the chip type is info */
    Info ,

    /* Indicates the chip type is warning */
    Warning ,
}