/**
 * Enum representing different types of feedback analysis prompts.
 */
export enum FeedbackAnalysisPromptCategory {

    /** Language recognition prompt type. */
    LanguageRecognition,

    /** Relevance analysis prompt type. */
    RelevanceAnalysis,

    /** Relevant content extraction prompt type. */
    RelevantContentExtraction,

    /** Entity identification prompt type. */
    EntityIdentification,

    /** Personal information redaction prompt type. */
    PersonalInformationRedaction,

    /** Opinion mining prompt type. */
    OpinionMining,

    /** Question points extraction prompt type. */
    QuestionPointsExtraction,

    /** Opinion points extraction prompt type. */
    OpinionPointsExtraction,

    /** Actionable opinions analysis prompt type. */
    ActionableOpinionsAnalysis,

    /** Actionable questions analysis prompt type. */
    ActionableQuestionsAnalysis,

    /** Opinions category analysis prompt type. */
    OpinionsCategoryAnalysis
}
